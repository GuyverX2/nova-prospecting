# ARCH-W1-001 — spike harness dependencies (not required by the service).
# Install in a separate venv; never add these to services/api dependencies.
PyYAML>=6,<7
# Optional live evaluation arms:
crawl4ai>=0.4.0
crawlee>=0.5.0
